# 华宽通智能体系统 - 系统设计说明书

> **版本**: V1.0 | **文档类型**: 逆向工程-系统设计说明 | **生成日期**: 2026-02-21

---

## 目录

1. [系统概述](#1-系统概述)
2. [技术架构](#2-技术架构)
3. [模块设计](#3-模块设计)
4. [数据库设计](#4-数据库设计)
5. [接口设计](#5-接口设计)
6. [部署架构](#6-部署架构)

---

## 1. 系统概述

### 1.1 系统背景

华宽通智能体系统是一个基于领域驱动设计（DDD）的智能硬件管理平台。系统以硬件销售为物理锚点，通过统一平台实现硬件数据价值最大化，并以AI驱动业务智能化。

### 1.2 系统目标

| 目标类型 | 具体目标 |
|---------|---------|
| **数据统一** | 构建统一的设备接入层，支持多种协议（MQTT、HTTP、CoAP） |
| **平台化** | 建立可扩展的中台能力，支撑多业务场景快速构建 |
| **智能化** | 实现规则引擎、工作流引擎、AI场景化应用 |
| **多租户** | 支持SaaS化运营，实现租户数据隔离和资源配额管理 |

### 1.3 系统范围

**包含范围**：设备接入与管理、多租户SaaS平台、规则引擎与工作流、Web管理平台

**不包含范围**：硬件产品研发与生产、第三方系统详细实现、BI工具深度定制开发、移动端APP开发

### 1.4 核心业务价值

1. **硬件数据化**：实现设备全生命周期数字化管理
2. **软件平台化**：构建可复用的中台能力
3. **AI场景化**：将AI能力嵌入具体业务场景

---

## 2. 技术架构

### 2.1 整体架构设计

系统采用**五层架构模型**：

```
┌─────────────────────────────────────────────────────────────────────┐
│ 价值呈现层: Web管理平台 (Vue 3 + TypeScript + Ant Design Vue)       │
├─────────────────────────────────────────────────────────────────────┤
│ 场景应用层: 智慧园区 | 智慧建筑 | 防霉管控 | 智慧畜牧 | 订阅服务    │
├─────────────────────────────────────────────────────────────────────┤
│ 能力中台层: 用户租户 | 设备管理 | 空间管理 | 规则引擎 | 工作流      │
├─────────────────────────────────────────────────────────────────────┤
│ 数据汇聚层: 设备接入服务 (MQTT → Kafka → 时序数据库/关系数据库)     │
├─────────────────────────────────────────────────────────────────────┤
│ 硬件部署层: 传感器 | 控制器 | 网关 | 智能硬件 | 第三方设备          │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

#### 后端技术栈

| 技术领域 | 技术选型 | 版本 |
|---------|---------|------|
| 开发语言 | Java | 17 |
| 核心框架 | Spring Boot | 3.2.5 |
| 微服务框架 | Spring Cloud Alibaba | 2023.0.1.0 |
| 服务网关 | Spring Cloud Gateway | 4.x |
| 注册中心 | Nacos | 2.3.0 |
| ORM框架 | MyBatis-Plus | 3.5.5 |
| 缓存 | Redis + Redisson | 7.2 / 3.27.1 |
| 消息队列 | RabbitMQ / Kafka | 3.12 / 3.7.0 |
| 物联网协议 | MQTT (EMQX) | 5.5.0 |
| 工作流引擎 | Camunda | 8.x |

#### 前端技术栈

| 技术领域 | 技术选型 | 版本 |
|---------|---------|------|
| 开发语言 | TypeScript | 5.x |
| 核心框架 | Vue | 3.x |
| 构建工具 | Vite | 5.x |
| UI组件库 | Ant Design Vue | 4.x |
| 状态管理 | Pinia | 2.x |

#### 数据存储

| 存储类型 | 技术选型 | 版本 |
|---------|---------|------|
| 关系数据库 | MySQL | 8.0 |
| 时序数据库 | InfluxDB / TDengine | 2.x / 3.x |
| 缓存 | Redis | 7.2 |
| 搜索引擎 | Elasticsearch | 8.12 |

### 2.3 分层架构设计

系统采用DDD分层架构：

- **Interface Layer (接口层)**：REST API Controller、WebSocket Handler、MQ Consumer
- **Application Layer (应用层)**：Application Service、Command Handler、Query Handler
- **Domain Layer (领域层)**：Aggregate Root、Entity、Value Object、Domain Event、Repository Interface
- **Infrastructure Layer (基础设施层)**：Repository Impl、External Service、MQ Producer

### 2.4 服务划分与限界上下文

| 服务名称 | 职责 |
|---------|------|
| hkt-iot-gateway | API网关：统一入口、路由、鉴权、限流 |
| hkt-iot-user-service | 用户管理、租户管理、RBAC权限、SSO/MFA |
| hkt-iot-device-service | 设备管理、物模型、设备控制、OTA升级、License |
| hkt-iot-space-service | 空间管理、资源绑定、逻辑分组 |
| hkt-iot-rule-service | 规则定义、规则解析、规则执行 |
| hkt-iot-workflow-service | 流程定义、流程实例、SLA监控、审批流程 |
| hkt-iot-notification-service | 通知模板、消息推送、重试机制 |
| hkt-iot-scene-service | 场景定义、场景联动、定时计划 |
| hkt-iot-smart-apps-service | 防霉管控、智慧畜牧 |
| hkt-iot-device-ingestion-service | 设备接入：MQTT→Kafka消息桥接 |

---

## 3. 模块设计

### 3.1 用户与租户管理模块

**核心功能**：
- 租户管理：多级组织架构（运营商→集团→子公司→企业）
- 用户管理：用户CRUD、用户状态管理、账户锁定/解锁
- RBAC权限：角色-权限-资源三级模型
- SSO单点登录：统一会话管理
- MFA多因素认证：TOTP、短信、邮件验证

**数据模型**：tenant → user → user_role → role → role_permission → permission

### 3.2 设备管理模块

**核心功能**：
- 设备注册：设备接入认证、证书管理
- 设备管理：设备CRUD、设备状态管理
- 物模型管理：设备属性、服务、事件定义
- 设备控制：单设备控制、批量控制（≤50台）
- 遥测数据：读写分离架构
- OTA升级：固件版本管理
- License管理：设备授权、到期提醒

**设备状态机**：INACTIVE → ONLINE ↔ OFFLINE ↔ FAULT → LOCKED → MAINTENANCE

**批量控制特性**：单次最多50台、5分钟超时、最多3次重试

### 3.3 空间管理模块

**空间层级**：PARK(园区) → BUILDING(楼栋) → FLOOR(楼层) → ROOM(房间)

**核心功能**：空间层级管理、空间拓扑、资源绑定、逻辑分组

### 3.4 规则引擎模块

**规则类型**：ALARM(告警)、LINKAGE(联动)、BILLING(计费)、CONTROL(控制)

**架构**：Lexer(词法分析) → Parser(语法分析) → Interpreter(解释执行)

**内置函数**：avg, sum, max, min, count, first, last

### 3.5 工作流引擎模块

**核心功能**：流程定义(BPMN 2.0)、流程实例、SLA监控、审批流程

**集成组件**：RuleEngineDelegate、NotificationListener、SLAMonitorListener

**消息队列**：workflow.process.started/completed、workflow.task.completed、workflow.sla.warning/breached

### 3.6 通知中心模块

**核心功能**：通知模板、多渠道推送(PUSH/EMAIL/SMS/IN_APP/WEBHOOK)

**可靠性保证**：幂等、指数退避重试(1min/5min/15min)、死信队列

### 3.7 智能应用模块

**防霉管控**：霉菌风险评估、自动湿度调节、告警通知

**智慧畜牧**：牲畜定位追踪、健康指标监测、越界告警

---

## 4. 数据库设计

### 4.1 数据库架构

采用**混合存储架构**：

- **MySQL 8.0**：业务数据（用户、租户、设备、空间、规则、工作流）
- **InfluxDB/TDengine**：时序数据（设备遥测、设备事件）
- **Redis 7.2**：缓存（会话、热点数据、分布式锁）
- **Elasticsearch 8.12**：日志（操作日志、审计日志）

### 4.2 核心表结构

#### 设备管理表

| 表名 | 说明 |
|------|------|
| device | 设备主表：id, tenant_id, device_sn, device_status, latest_properties |
| device_thing_model | 物模型表：properties_def, services_def, events_def |
| device_license | License表：license_key, license_type, end_date |
| device_telemetry_snapshot | 遥测快照表：telemetry_data, event_time |

#### 用户租户表

| 表名 | 说明 |
|------|------|
| tenant | 租户表：tenant_code, tenant_type, parent_tenant_id |
| user | 用户表：username, password, is_mfa_enabled |
| role | 角色表：role_code, role_type |
| permission | 权限表：permission_code, resource_type, action |

#### 规则引擎表

| 表名 | 说明 |
|------|------|
| rule | 规则表：rule_type, trigger_type, rule_config |
| rule_condition | 规则条件表：condition_type, condition_config |
| rule_action | 规则动作表：action_type, action_config |
| rule_execution_log | 执行日志表（按月分区） |

### 4.3 时序数据库设计

**InfluxDB Measurement**：

- device_telemetry：设备遥测数据
  - Tags: tenant_id, device_id, device_sn, device_type, space_id
  - Fields: temperature, humidity, pressure, etc.

- device_event：设备事件数据
  - Tags: tenant_id, device_id, event_type, event_level
  - Fields: event_message, event_code

**数据保留策略**：hot_rp(7天) → warm_rp(30天) → cold_rp(365天)

### 4.4 分库分表策略

- **分库键**：tenant_id
- **分表策略**：device按tenant_id分表，rule_execution_log按时间分区

---

## 5. 接口设计

### 5.1 API设计规范

**URL规范**：`/api/v1/{resource}`

**响应格式**：
```typescript
interface ApiResponse<T> {
  code: string
  message: string
  data: T
  timestamp: number
}
```

### 5.2 核心API接口

#### 设备管理API

| 接口 | 方法 | 路径 |
|------|------|------|
| 设备列表 | GET | /v1/devices |
| 设备详情 | GET | /v1/devices/{id} |
| 创建设备 | POST | /v1/devices |
| 批量控制 | POST | /v1/devices/batch-control |

#### 遥测数据API

| 接口 | 方法 | 路径 |
|------|------|------|
| 最新遥测 | GET | /v1/telemetry/{deviceId}/latest |
| 历史遥测 | POST | /v1/telemetry/history |

#### 规则引擎API

| 接口 | 方法 | 路径 |
|------|------|------|
| 规则列表 | GET | /v1/rules |
| 执行规则 | POST | /v1/rules/{id}/execute |

### 5.3 WebSocket实时通信

**事件类型**：
- device.online/offline/status_change/telemetry
- alarm.created/cleared
- rule.triggered
- scenario.executed

**心跳机制**：ping/pong，间隔30秒

---

## 6. 部署架构

### 6.1 容器化部署

**Docker Compose服务**：

| 服务 | 端口 | 说明 |
|------|------|------|
| mysql | 3306 | MySQL 8.0 |
| redis | 6379 | Redis 7.2 |
| rabbitmq | 5672/15672 | RabbitMQ 3.12 |
| emqx | 1883/18083 | EMQX 5.5.0 |
| nacos | 8848 | Nacos 2.3.0 |
| elasticsearch | 9200 | ES 8.12 |
| prometheus | 9090 | Prometheus 2.49 |
| grafana | 3000 | Grafana 10.3 |
| zipkin | 9411 | Zipkin 3.0.5 |

### 6.2 监控架构

```
应用指标 → Prometheus → Grafana
日志 → Filebeat → Elasticsearch → Kibana
链路 → Zipkin
```

### 6.3 告警规则

| 告警类型 | 规则 | 级别 |
|---------|------|------|
| 服务可用性 | 服务实例下线 | Critical |
| 响应时间 | P99 > 1s | Warning |
| 错误率 | 错误率 > 1% | Warning |

---

## 附录

### 错误码汇总

| 范围 | 模块 |
|------|------|
| 1xxx | 通用错误 |
| 2xxx | 认证授权错误 |
| 31xx | 租户/用户错误 |
| 32xx | 设备错误 |
| 33xx | 空间错误 |
| 34xx | 规则引擎错误 |

### 设备类型枚举

WATER_METER, ELECTRIC_METER, GAS_METER, SMOKE_DETECTOR, TEMPERATURE_SENSOR, HUMIDITY_SENSOR, DOOR_LOCK, AIR_CONDITIONER, LIGHT, GATEWAY

---

> 本文档基于项目代码逆向分析生成
