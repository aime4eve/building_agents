## =====================================================================
## Device Ingestion Service Configuration
## 设备接入服务配置
## =====================================================================

server:
  port: 8084
  servlet:
    context-path: /api/v1/device-ingestion

spring:
  application:
    name: device-ingestion-service

  ## Kafka配置
  kafka:
    bootstrap-servers: kafka1:9092,kafka2:9092,kafka3:9092
    consumer:
      ## 消费者组配置
      group-id: device-ingestion-service
      ## 自动提交关闭，手动控制
      enable-auto-commit: false
      ## 从最新消息开始消费
      auto-offset-reset: latest
      ## Key反序列化器
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      ## Value反序列化器
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      ## 最大拉取记录数
      max-poll-records: 100
      ## 最大拉取间隔
      max-poll-interval-ms: 300000
      ## 会话超时
      session-timeout-ms: 30000
      ## 心跳间隔
      heartbeat-interval-ms: 10000
    properties:
      ## 分区分配策略
      partition.assignment.strategy: org.apache.kafka.clients.consumer.StickyAssignor
      ## 请求超时
      request.timeout.ms: 30000
      ## 重试配置
      retries: 3

  ## Redis配置（设备状态缓存）
  redis:
    host: redis
    port: 6379
    database: 0
    password: ${REDIS_PASSWORD}
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: 2000ms
    timeout: 3000ms

  ## 数据源配置（MySQL）
  datasource:
    url: jdbc:mysql://mysql:3306/hkt_iot?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  ## JPA配置
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true

## =====================================================================
## InfluxDB配置
## =====================================================================
influx:
  url: http://influxdb:8086
  token: ${INFLUX_TOKEN}
  org: hkt_iot
  bucket:
    raw: hkt_iot_raw
    event: hkt_iot_event
    status: hkt_iot_status
  write:
    ## 批量写入大小
    batch-size: 1000
    ## 刷新间隔
    flush-interval: 1000
    ## 缓存大小
    buffer-limit: 10000
  read:
    ## 查询超时
    timeout: 30000

## =====================================================================
## 业务配置
## =====================================================================
device:
  ## 设备注册配置
  registration:
    ## 设备证书有效期（天）
    cert-expiry-days: 365
    ## JWT Token有效期（秒）
    token-expiry-seconds: 86400
    ## RefreshToken有效期（秒）
    refresh-token-expiry-seconds: 2592000

  ## 心跳配置
  heartbeat:
    ## 心跳间隔（秒）
    interval: 60
    ## 心跳超时阈值（秒）
    timeout: 180

  ## 设备状态缓存配置
  cache:
    ## 缓存过期时间（秒）
    ttl: 300

  ## 限流配置
  rate-limit:
    ## 设备注册限流（每分钟请求数）
    register-per-minute: 10
    ## API调用限流（每秒请求数）
    api-per-second: 100

## =====================================================================
## 管理端点配置
## =====================================================================
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}

## =====================================================================
## 日志配置
## =====================================================================
logging:
  level:
    root: INFO
    com.huakuangtong.iot.ingestion: DEBUG
    org.springframework.kafka: INFO
    org.apache.kafka: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: /var/log/device-ingestion-service/application.log
    max-size: 100MB
    max-history: 30

## =====================================================================
## 任务调度配置
## =====================================================================
scheduling:
  enabled: true
  pool-size: 5
  tasks:
    ## 心跳检查任务
    heartbeat-check:
      enabled: true
      cron: "0 * * * * ?"
    ## 设备状态清理任务
    status-cleanup:
      enabled: true
      cron: "0 0 2 * * ?"
