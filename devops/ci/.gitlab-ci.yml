# =============================================================================
# 华宽通智能体系统 - GitLab CI/CD 流水线配置
# =============================================================================
# 版本: 1.0.0
# 说明: 完整的CI/CD流水线，包含代码检查、测试、构建、部署
# =============================================================================

# 定义流水线阶段
stages:
  - build       # 代码编译
  - test        # 代码测试
  - security    # 安全扫描
  - package     # 打包镜像
  - deploy-dev  # 部署到开发环境
  - deploy-test # 部署到测试环境
  - deploy-prod # 部署到生产环境（需手动审批）

# 定义全局变量
variables:
  # Docker镜像相关
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"
  IMAGE_NAME: ${CI_REGISTRY_IMAGE}
  IMAGE_TAG: ${CI_COMMIT_SHORT_SHA}
  LATEST_TAG: ${CI_COMMIT_REF_NAME}

  # Maven相关
  MAVEN_OPTS: "-Dmaven.repo.local=${CI_PROJECT_DIR}/.m2/repository"
  MAVEN_CLI_OPTS: "--batch-mode --errors --fail-at-end --show-version"

  # Java相关
  JAVA_VERSION: "17"

# 缓存配置
.cache_template: &cache_config
  key:
    files:
      - pom.xml
    prefix: ${CI_COMMIT_REF_SLUG}
  paths:
    - .m2/repository/
    - node_modules/

# 定义镜像模板
.docker_template: &docker_config
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin $CI_REGISTRY

# 定义Maven模板
.maven_template: &maven_config
  image: maven:3.9.5-eclipse-temurin-17
  cache:
    <<: *cache_config

# =============================================================================
# 阶段1: 代码编译
# =============================================================================
compile:
  stage: build
  <<: *maven_config
  script:
    - echo "编译Java代码..."
    - mvn $MAVEN_CLI_OPTS clean compile
  artifacts:
    paths:
      - target/
    expire_in: 1 hour
  only:
    - merge_requests
    - main
    - develop
  tags:
    - docker

# =============================================================================
# 阶段2: 代码测试
# =============================================================================
# 单元测试
unit-test:
  stage: test
  <<: *maven_config
  script:
    - echo "运行单元测试..."
    - mvn $MAVEN_CLI_OPTS test
    - echo "生成测试覆盖率报告..."
    - mvn jacoco:report
  coverage: '/Total.*?([0-9]{1,3})%/'
  artifacts:
    paths:
      - target/site/jacoco/
      - target/surefire-reports/
    reports:
      junit: target/surefire-reports/TEST-*.xml
    coverage_report:
      coverage_format: jacoco
      path: target/site/jacoco/jacoco.xml
    expire_in: 1 week
  only:
    - merge_requests
    - main
    - develop
  tags:
    - docker

# 集成测试
integration-test:
  stage: test
  <<: *maven_config
  services:
    - mysql:8.0
    - redis:7.2-alpine
    - rabbitmq:3.12-management-alpine
  variables:
    MYSQL_ROOT_PASSWORD: root123456
    MYSQL_DATABASE: huakuantong_test
    RABBITMQ_DEFAULT_USER: admin
    RABBITMQ_DEFAULT_PASS: admin123456
  script:
    - echo "运行集成测试..."
    - mvn $MAVEN_CLI_OPTS verify -DskipUnitTests
  artifacts:
    reports:
      junit: target/failsafe-reports/TEST-*.xml
    expire_in: 1 week
  only:
    - merge_requests
    - main
    - develop
  tags:
    - docker

# 代码质量检查
code-quality:
  stage: test
  <<: *maven_config
  script:
    - echo "运行代码质量检查..."
    - mvn $MAVEN_CLI_OPTS sonar:sonar
      -Dsonar.projectKey=${CI_PROJECT_NAME}
      -Dsonar.host.url=${SONAR_HOST}
      -Dsonar.login=${SONAR_TOKEN}
      -Dsonar.qualitygate.wait=true
  allow_failure: true
  only:
    - merge_requests
    - main
    - develop
  tags:
    - docker

# =============================================================================
# 阶段3: 安全扫描
# =============================================================================
# 依赖漏洞扫描
dependency-scan:
  stage: security
  <<: *maven_config
  script:
    - echo "扫描依赖漏洞..."
    - mvn $MAVEN_CLI_OPTS org.owasp:dependency-check-maven:check
  artifacts:
    paths:
      - target/dependency-check-report.html
    expire_in: 1 week
  allow_failure: true
  only:
    - merge_requests
    - main
    - develop
  tags:
    - docker

# 容器镜像扫描
container-scan:
  stage: security
  <<: *docker_config
  script:
    - echo "扫描容器镜像漏洞..."
    - docker pull ${IMAGE_NAME}:${IMAGE_TAG}
    - trivy image --format json --output trivy-report.json ${IMAGE_NAME}:${IMAGE_TAG}
  artifacts:
    paths:
      - trivy-report.json
    expire_in: 1 week
  allow_failure: true
  needs:
    - package:backend
  only:
    - main
    - develop
  tags:
    - docker

# =============================================================================
# 阶段4: 打包镜像
# =============================================================================
# 后端服务镜像
package:backend:
  stage: package
  <<: *docker_config
  script:
    - echo "构建后端服务Docker镜像..."
    - mvn $MAVEN_CLI_OPTS package -DskipTests
    - docker build -t ${IMAGE_NAME}/backend:${IMAGE_TAG} -f docker/backend/Dockerfile .
    - docker tag ${IMAGE_NAME}/backend:${IMAGE_TAG} ${IMAGE_NAME}/backend:${LATEST_TAG}
    - docker push ${IMAGE_NAME}/backend:${IMAGE_TAG}
    - docker push ${IMAGE_NAME}/backend:${LATEST_TAG}
  only:
    - main
    - develop
  tags:
    - docker

# 前端应用镜像
package:frontend:
  stage: package
  image: node:20-alpine
  cache:
    <<: *cache_config
  script:
    - echo "构建前端应用..."
    - cd frontend
    - npm ci
    - npm run build
    - cd ..
    - docker build -t ${IMAGE_NAME}/frontend:${IMAGE_TAG} -f docker/frontend/Dockerfile .
    - docker tag ${IMAGE_NAME}/frontend:${IMAGE_TAG} ${IMAGE_NAME}/frontend:${LATEST_TAG}
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin $CI_REGISTRY
    - docker push ${IMAGE_NAME}/frontend:${IMAGE_TAG}
    - docker push ${IMAGE_NAME}/frontend:${LATEST_TAG}
  only:
    - main
    - develop
  tags:
    - docker

# =============================================================================
# 阶段5: 部署到开发环境
# =============================================================================
deploy:dev:
  stage: deploy-dev
  image: bitnami/kubectl:latest
  environment:
    name: development
    url: http://dev.huakuantong.com
  script:
    - echo "部署到开发环境..."
    - kubectl config use-context dev-cluster
    - kubectl set image deployment/backend
        backend=${IMAGE_NAME}/backend:${IMAGE_TAG}
        -n huakuantong-dev
    - kubectl set image deployment/frontend
        frontend=${IMAGE_NAME}/frontend:${IMAGE_TAG}
        -n huakuantong-dev
    - kubectl rollout status deployment/backend -n huakuantong-dev
    - kubectl rollout status deployment/frontend -n huakuantong-dev
  when: on_success
  only:
    - develop
  tags:
    - docker

# =============================================================================
# 阶段6: 部署到测试环境
# =============================================================================
deploy:test:
  stage: deploy-test
  image: bitnami/kubectl:latest
  environment:
    name: testing
    url: http://test.huakuantong.com
  script:
    - echo "部署到测试环境..."
    - kubectl config use-context test-cluster
    - kubectl set image deployment/backend
        backend=${IMAGE_NAME}/backend:${IMAGE_TAG}
        -n huakuantong-test
    - kubectl set image deployment/frontend
        frontend=${IMAGE_NAME}/frontend:${IMAGE_TAG}
        -n huakuantong-test
    - kubectl rollout status deployment/backend -n huakuantong-test
    - kubectl rollout status deployment/frontend -n huakuantong-test
  when: manual
  only:
    - main
  tags:
    - docker

# =============================================================================
# 阶段7: 部署到生产环境
# =============================================================================
deploy:prod:
  stage: deploy-prod
  image: bitnami/kubectl:latest
  environment:
    name: production
    url: https://www.huakuantong.com
  script:
    - echo "部署到生产环境..."
    - kubectl config use-context prod-cluster
    # 灰度发布 - 先更新10%的Pod
    - kubectl patch deployment backend -n huakuantong-prod
        -p '{"spec":{"strategy":{"rollingUpdate":{"maxSurge":"10%","maxUnavailable":"0"}}}}'
    - kubectl set image deployment/backend
        backend=${IMAGE_NAME}/backend:${IMAGE_TAG}
        -n huakuantong-prod
    - kubectl set image deployment/frontend
        frontend=${IMAGE_NAME}/frontend:${IMAGE_TAG}
        -n huakuantong-prod
    - kubectl rollout status deployment/backend -n huakuantong-prod
    - kubectl rollout status deployment/frontend -n huakuantong-prod
  when: manual
  only:
    - main
  tags:
    - docker

# 通知配置（可选）
notify:
  stage: .post
  image: alpine:latest
  script:
    - echo "发送部署通知..."
    # 可添加企业微信/钉钉通知逻辑
  when: always
  only:
    - main
    - develop
  tags:
    - docker
