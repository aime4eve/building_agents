## =====================================================================
## Docker Compose Configuration
## 华宽通智能体系统 - 设备接入层 (EMQX + Kafka)
## =====================================================================
## 版本: 1.1.0
## 更新日期: 2026-02-20
## 说明: 根据设备接入层架构设计说明书配置
## =====================================================================

version: '3.8'

services:
  ## ----------------------------------------------------------------------
  ## Zookeeper (Kafka依赖)
  ## ----------------------------------------------------------------------
  zookeeper:
    image: bitnami/zookeeper:3.8
    container_name: hkt-zookeeper
    hostname: zookeeper
    ports:
      - "2181:2181"
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    volumes:
      - zookeeper_data:/bitnami/zookeeper
    networks:
      - hkt_iot_network

  ## ----------------------------------------------------------------------
  ## Kafka Broker 1
  ## ----------------------------------------------------------------------
  kafka1:
    image: bitnami/kafka:3.5
    container_name: hkt-kafka1
    hostname: kafka1
    ports:
      - "9092:9092"
      - "19092:19092"
    environment:
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_CFG_BROKER_ID=1
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,EXTERNAL://:19092
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka1:9092,EXTERNAL://localhost:19092
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - KAFKA_CFG_LOG_RETENTION_HOURS=168
      - KAFKA_CFG_LOG_SEGMENT_BYTES=1073741824
      - KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE=true
      - KAFKA_CFG_DEFAULT_REPLICATION_FACTOR=3
      - KAFKA_CFG_NUM_PARTITIONS=10
      - KAFKA_CFG_LOG_DIRS=/bitnami/kafka/logs
    volumes:
      - kafka1_data:/bitnami/kafka
    depends_on:
      - zookeeper
    networks:
      - hkt_iot_network

  ## ----------------------------------------------------------------------
  ## Kafka Broker 2
  ## ----------------------------------------------------------------------
  kafka2:
    image: bitnami/kafka:3.5
    container_name: hkt-kafka2
    hostname: kafka2
    ports:
      - "9093:9093"
      - "19093:19093"
    environment:
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_CFG_BROKER_ID=2
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9093,EXTERNAL://:19093
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka2:9093,EXTERNAL://localhost:19093
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - KAFKA_CFG_LOG_RETENTION_HOURS=168
      - KAFKA_CFG_LOG_SEGMENT_BYTES=1073741824
      - KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE=true
      - KAFKA_CFG_DEFAULT_REPLICATION_FACTOR=3
      - KAFKA_CFG_NUM_PARTITIONS=10
      - KAFKA_CFG_LOG_DIRS=/bitnami/kafka/logs
    volumes:
      - kafka2_data:/bitnami/kafka
    depends_on:
      - zookeeper
    networks:
      - hkt_iot_network

  ## ----------------------------------------------------------------------
  ## Kafka Broker 3
  ## ----------------------------------------------------------------------
  kafka3:
    image: bitnami/kafka:3.5
    container_name: hkt-kafka3
    hostname: kafka3
    ports:
      - "9094:9094"
      - "19094:19094"
    environment:
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_CFG_BROKER_ID=3
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9094,EXTERNAL://:19094
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka3:9094,EXTERNAL://localhost:19094
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - KAFKA_CFG_LOG_RETENTION_HOURS=168
      - KAFKA_CFG_LOG_SEGMENT_BYTES=1073741824
      - KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE=true
      - KAFKA_CFG_DEFAULT_REPLICATION_FACTOR=3
      - KAFKA_CFG_NUM_PARTITIONS=10
      - KAFKA_CFG_LOG_DIRS=/bitnami/kafka/logs
    volumes:
      - kafka3_data:/bitnami/kafka
    depends_on:
      - zookeeper
    networks:
      - hkt_iot_network

  ## ----------------------------------------------------------------------
  ## EMQX Broker 1
  ## ----------------------------------------------------------------------
  emqx1:
    image: emqx/emqx:5.5.0
    container_name: hkt-emqx1
    hostname: emqx1
    ports:
      - "1883:1883"      # MQTT
      - "8883:8883"      # MQTT/SSL
      - "8083:8083"      # MQTT/WS
      - "8084:8084"      # MQTT/WSS
      - "18083:18083"    # Dashboard
    environment:
      - EMQX_NAME=emqx
      - EMQX_HOST=127.0.0.1
      - TZ=Asia/Shanghai
    volumes:
      - ./emqx.conf:/opt/emqx/etc/emqx.conf:ro
      - ./acl.conf:/opt/emqx/etc/acl.conf:ro
      - ./certs:/etc/emqx/certs:ro
      - emqx1_data:/opt/emqx/data
      - emqx1_log:/opt/emqx/log
    depends_on:
      - kafka1
      - kafka2
      - kafka3
    networks:
      - hkt_iot_network
    healthcheck:
      test: ["CMD", "emqx", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  ## ----------------------------------------------------------------------
  ## EMQX Broker 2 (可选，用于集群)
  ## ----------------------------------------------------------------------
  emqx2:
    image: emqx/emqx:5.5.0
    container_name: hkt-emqx2
    hostname: emqx2
    ports:
      - "1884:1883"
      - "8884:8883"
      - "8085:8083"
      - "8086:8084"
      - "18084:18083"
    environment:
      - EMQX_NAME=emqx
      - EMQX_HOST=emqx2
      - TZ=Asia/Shanghai
    volumes:
      - ./emqx.conf:/opt/emqx/etc/emqx.conf:ro
      - ./acl.conf:/opt/emqx/etc/acl.conf:ro
      - ./certs:/etc/emqx/certs:ro
      - emqx2_data:/opt/emqx/data
      - emqx2_log:/opt/emqx/log
    depends_on:
      - kafka1
      - kafka2
      - kafka3
      - emqx1
    networks:
      - hkt_iot_network
    healthcheck:
      test: ["CMD", "emqx", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  ## ----------------------------------------------------------------------
  ## Kafka Exporter (Prometheus监控)
  ## ----------------------------------------------------------------------
  kafka-exporter:
    image: danielqsj/kafka-exporter:latest
    container_name: hkt-kafka-exporter
    ports:
      - "9308:9308"
    environment:
      - KAFKA_BROKER_VERSION=2.0.0
    command:
      - '--kafka.server=kafka1:9092'
      - '--kafka.server=kafka2:9093'
      - '--kafka.server=kafka3:9094'
      - '--web.listen-address=:9308'
      - '--log.level=info'
      - '--tls.enabled=false'
      - '--sasl.enabled=false'
    depends_on:
      - kafka1
      - kafka2
      - kafka3
    networks:
      - hkt_iot_network

  ## ----------------------------------------------------------------------
  ## Kafka UI (可选)
  ## ----------------------------------------------------------------------
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: hkt-kafka-ui
    ports:
      - "8080:8080"
    environment:
      - KAFKA_CLUSTERS_0_NAME=HKT IoT Cluster
      - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka1:9092,kafka2:9093,kafka3:9094
      - KAFKA_CLUSTERS_0_ZOOKEEPER=zookeeper:2181
    depends_on:
      - kafka1
      - kafka2
      - kafka3
    networks:
      - hkt_iot_network

## ----------------------------------------------------------------------
## Volumes
## ----------------------------------------------------------------------
volumes:
  zookeeper_data:
    driver: local
  kafka1_data:
    driver: local
  kafka2_data:
    driver: local
  kafka3_data:
    driver: local
  emqx1_data:
    driver: local
  emqx1_log:
    driver: local
  emqx2_data:
    driver: local
  emqx2_log:
    driver: local

## ----------------------------------------------------------------------
## Networks
## ----------------------------------------------------------------------
networks:
  hkt_iot_network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

## ----------------------------------------------------------------------
## 使用说明
## ----------------------------------------------------------------------
##
## 启动服务:
##   docker-compose -f docker-compose-mqttkafka.yml up -d
##
## 查看日志:
##   docker-compose -f docker-compose-mqttkafka.yml logs -f emqx1
##
## 创建Kafka Topics:
##   chmod +x kafka/create-topics.sh
##   docker exec -i hkt-kafka1 bash < kafka/create-topics.sh
##
## 访问EMQX Dashboard:
##   URL: http://localhost:18083
##   默认用户: admin
##   默认密码: public
##
## 访问Kafka UI:
##   URL: http://localhost:8080
##
## 停止服务:
##   docker-compose -f docker-compose-mqttkafka.yml down
##
## 清理所有数据:
##   docker-compose -f docker-compose-mqttkafka.yml down -v
##
## ----------------------------------------------------------------------
