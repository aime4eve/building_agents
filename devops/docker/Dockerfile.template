# ============================================================================
# 华宽通智能体系统 - Spring Boot微服务 Dockerfile模板
# 版本: V1.0
# 创建日期: 2026-02-20
# 说明: 通用的Spring Boot微服务Dockerfile模板
# ============================================================================

# --------------------------------------------------------------------------
# 多阶段构建 - 第一阶段: Maven构建
# --------------------------------------------------------------------------
FROM maven:3.9-eclipse-temurin-17-alpine AS builder

# 设置工作目录
WORKDIR /app

# 复制 Maven 配置文件 (可选，包含公司私有仓库配置)
# COPY mvn-settings.xml /root/.m2/settings.xml

# 复制 pom.xml 并下载依赖 (利用Docker缓存层)
COPY pom.xml .
RUN mvn dependency:go-offline -B

# 复制源代码
COPY src ./src

# 构建应用 (跳过测试以加快构建速度)
RUN mvn clean package -DskipTests -B

# --------------------------------------------------------------------------
# 多阶段构建 - 第二阶段: 运行时镜像
# --------------------------------------------------------------------------
FROM eclipse-temurin:17-jre-alpine

# 设置维护者信息
LABEL maintainer="devops@huakuangtong.com"
LABEL description="HKT IoT Microservice"
LABEL version="1.0.0"

# 设置时区
ENV TZ=Asia/Shanghai
RUN apk add --no-cache tzdata && \
    cp /usr/share/zoneinfo/${TZ} /etc/localtime && \
    echo ${TZ} > /etc/timezone && \
    apk del tzdata

# 创建应用用户 (非root用户运行)
RUN addgroup -S appgroup && \
    adduser -S appuser -G appgroup

# 设置工作目录
WORKDIR /app

# 从构建阶段复制JAR文件
COPY --from=builder /app/target/*.jar app.jar

# 创建日志目录
RUN mkdir -p /app/logs && \
    chown -R appuser:appgroup /app

# 切换到非root用户
USER appuser

# --------------------------------------------------------------------------
# JVM参数优化
# --------------------------------------------------------------------------
ENV JAVA_OPTS="-Xms512m -Xmx1024m \
               -XX:+UseG1GC \
               -XX:MaxGCPauseMillis=200 \
               -XX:+HeapDumpOnOutOfMemoryError \
               -XX:HeapDumpPath=/app/logs/heapdump.hprof \
               -XX:+PrintGCDetails \
               -XX:+PrintGCDateStamps \
               -Xloggc:/app/logs/gc.log \
               -Djava.security.egd=file:/dev/./urandom \
               -Dspring.profiles.active=prod"

# --------------------------------------------------------------------------
# 健康检查
# --------------------------------------------------------------------------
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost:8080/actuator/health || exit 1

# 暴露端口 (微服务默认端口)
EXPOSE 8080

# 挂载日志目录
VOLUME ["/app/logs"]

# 启动应用
ENTRYPOINT ["sh", "-c", "java ${JAVA_OPTS} -jar /app/app.jar"]
