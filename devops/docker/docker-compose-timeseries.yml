# ============================================================================
# 华宽通智能体系统 - 时序数据库 Docker Compose 配置
# 版本: V1.0
# 创建日期: 2026-02-20
# ============================================================================

version: '3.8'

services:
  # ===========================================================================
  # InfluxDB 2.x - 时序数据库
  # ===========================================================================
  influxdb:
    image: influxdb:2.7
    container_name: hkt-influxdb
    ports:
      - "8086:8086"  # HTTP API
    environment:
      # Docker特定的认证配置
      - DOCKER_INFLUXDB_INIT_MODE=setup
      - DOCKER_INFLUXDB_INIT_USERNAME=admin
      - DOCKER_INFLUXDB_INIT_PASSWORD=admin123456
      - DOCKER_INFLUXDB_INIT_ORG=huakuantong
      - DOCKER_INFLUXDB_INIT_BUCKET=huakuantong_telemetry
      - DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=my-super-secret-auth-token
      - DOCKER_INFLUXDB_INIT_RETENTION=90d
    volumes:
      # 数据持久化
      - influxdb-data:/var/lib/influxdb2
      - influxdb-config:/etc/influxdb
    networks:
      - hkt-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "influx", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ===========================================================================
  # InfluxDB Web UI (可选)
  # ===========================================================================
  influxdb-ui:
    image: influxdb-ui:latest
    container_name: hkt-influxdb-ui
    ports:
      - "8088:8088"
    environment:
      - INFLUXDB_URL=http://influxdb:8086
      - INFLUXDB_TOKEN=my-super-secret-auth-token
      - INFLUXDB_ORG=huakuantong
    depends_on:
      - influxdb
    networks:
      - hkt-network
    restart: unless-stopped
    profiles:
      - ui  # 使用 --profile ui 启用

  # ===========================================================================
  # TDengine 3.x - 时序数据库（备选方案）
  # ===========================================================================
  tdengine:
    image: tdengine/tdengine:3.0.3.0
    container_name: hkt-tdengine
    ports:
      - "6030:6030"  # TDengine TCP
      - "6041:6041"  # TDengine RESTful
      - "6043-6049:6043-6049"  # TDengine 数据节点端口
    environment:
      - TAOS_FQDN=tdengine
      - TAOS_FIRST_EP=tdengine:6030
      - TAOS_SERVER_PORT=6030
    volumes:
      - tdengine-data:/var/lib/taos
      - tdengine-config:/etc/taos
      - ./tdengine/taos.cfg:/etc/taos/taos.cfg:ro
    networks:
      - hkt-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "taos", "-s", "show databases;"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    profiles:
      - tdengine  # 使用 --profile tdengine 启用（与InfluxDB二选一）

  # ===========================================================================
  # Grafana - 数据可视化（可选）
  # ===========================================================================
  grafana:
    image: grafana/grafana:10.0.0
    container_name: hkt-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_INSTALL_PLUGINS=grafana-clock-panel
    volumes:
      - grafana-data:/var/lib/grafana
      - grafana-logs:/var/log/grafana
      - grafana-provisioning:/etc/grafana/provisioning
    networks:
      - hkt-network
    depends_on:
      - influxdb
    restart: unless-stopped
    profiles:
      - monitoring  # 使用 --profile monitoring 启用

  # ===========================================================================
  # Redis - 缓存（共享服务）
  # ===========================================================================
  redis:
    image: redis:7-alpine
    container_name: hkt-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --requirepass redis123
    volumes:
      - redis-data:/data
    networks:
      - hkt-network
    restart: unless-stopped

  # ===========================================================================
  # MySQL - 关系数据库（共享服务）
  # ===========================================================================
  mysql:
    image: mysql:8.0
    container_name: hkt-mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root123
      - MYSQL_DATABASE=huakuantong_agent
      - MYSQL_USER=huakuantong
      - MYSQL_PASSWORD=hkt123456
      - TZ=Asia/Shanghai
    volumes:
      - mysql-data:/var/lib/mysql
      - ./mysql/init:/docker-entrypoint-initdb.d:ro
    networks:
      - hkt-network
    restart: unless-stopped
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

volumes:
  influxdb-data:
    driver: local
  influxdb-config:
    driver: local
  tdengine-data:
    driver: local
  tdengine-config:
    driver: local
  grafana-data:
    driver: local
  grafana-logs:
    driver: local
  grafana-provisioning:
    driver: local
  redis-data:
    driver: local
  mysql-data:
    driver: local

networks:
  hkt-network:
    driver: bridge
