# =============================================================================
# Kibana 配置文件 - 本地开发环境
# =============================================================================

# 服务器配置
server.host: "0.0.0.0"
server.port: 5601
server.name: "huakuantong-kibana"

# Elasticsearch配置
elasticsearch.hosts: ["http://elasticsearch:9200"]

# 国际化配置
i18n.locale: "zh-CN"

# 监控配置
monitoring.ui.container.elasticsearch.enabled: true

# 日志配置
logging.appenders:
  console:
    type: console
    layout:
      type: json
  file:
    type: file
    fileName: /var/log/kibana/kibana.log
    layout:
      type: json

logging.root:
  appenders:
    - default
    - file
  level: info

# 安全配置（开发环境禁用）
xpack.security.enabled: false

# APM配置
xpack.apm.ui.enabled: true

# ML配置
xpack.ml.enabled: false

# 告警配置
xpack.alerting.enabled: true
xpack.alerting.encryptionKey: "huakuantong_alerting_encryption_key_32chars"

# Actions配置
xpack.actions.enabled: true
xpack.actions.allowedHosts:
  - "*"

xpack.actions.allowedConnectorTypes:
  - .*
