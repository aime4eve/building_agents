# ============================================================================
# 华宽通智能体系统 - Spring Boot 数据库配置
# 版本: V1.0
# 说明: 开发环境数据库连接配置
# ============================================================================

spring:
  # --------------------------------------------------------------------------
  # 数据源配置
  # --------------------------------------------------------------------------
  datasource:
    # 主数据源配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hkt_iot?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123456

    # HikariCP 连接池配置
    hikari:
      # 连接池配置
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1

      # 连接属性
      pool-name: HktIotHikariPool
      auto-commit: true

      # 性能优化
      register-mbeans: true

  # --------------------------------------------------------------------------
  # 动态多数据源配置 (如需使用多数据源)
  # --------------------------------------------------------------------------
  dynamic:
    datasource:
      # 设备管理数据源
      device:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/hkt_iot_device?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: root123456

      # 空间管理数据源
      space:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/hkt_iot_space?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: root123456

      # 用户租户数据源
      user:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/hkt_iot_user?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: root123456

      # 规则引擎数据源
      rule:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/hkt_iot_rule?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: root123456

  # --------------------------------------------------------------------------
  # JPA/Hibernate 配置
  # --------------------------------------------------------------------------
  jpa:
    # 数据库方言
    database-platform: org.hibernate.dialect.MySQL8Dialect
    # 显示SQL
    show-sql: true
    # 格式化SQL
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        jdbc:
          batch_size: 50
          order_inserts: true
          order_updates: true
        # 二级缓存配置
        cache:
          use_second_level_cache: true
          use_query_cache: true
          region:
            factory_class: org.hibernate.cache.jcache.JCacheRegionFactory
    # 自动更新DDL (开发环境使用update，生产环境使用validate或none)
    hibernate:
      ddl-auto: validate

  # --------------------------------------------------------------------------
  # Redis 配置
  # --------------------------------------------------------------------------
  redis:
    host: localhost
    port: 6379
    password:
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        # 连接池最大连接数
        max-active: 20
        # 连接池最大阻塞等待时间
        max-wait: -1ms
        # 连接池中的最大空闲连接
        max-idle: 10
        # 连接池中的最小空闲连接
        min-idle: 5
      # 关闭超时时间
      shutdown-timeout: 100ms

  # --------------------------------------------------------------------------
  # Redisson 配置 (分布式锁、缓存等)
  # --------------------------------------------------------------------------
  redisson:
    config: |
      singleServerConfig:
        address: "redis://localhost:6379"
        database: 0
        password: null
        connectionPoolSize: 20
        connectionMinimumIdleSize: 5
        idleConnectionTimeout: 10000
        timeout: 3000
        retryAttempts: 3
        retryInterval: 1500
      threads: 16
      nettyThreads: 32

# --------------------------------------------------------------------------
# MyBatis-Plus 配置
# --------------------------------------------------------------------------
mybatis-plus:
  # mapper文件位置
  mapper-locations: classpath*:/mapper/**/*.xml
  # 实体类扫描包
  type-aliases-package: com.huakuangtong.iot.**.entity
  # 配置
  configuration:
    # 驼峰命名转换
    map-underscore-to-camel-case: true
    # 日志实现
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    # 开启二级缓存
    cache-enabled: true
    # 分页参数合理化
    reasonable: true
  # 全局配置
  global:
    # 数据库ID类型 (ASSIGN_ID: 雪花算法, ASSIGN_UUID: UUID)
    db-config:
      id-type: ASSIGN_ID
      # 逻辑删除字段
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      # 字段验证策略
      field-strategy: NOT_NULL

# --------------------------------------------------------------------------
# 分页配置
# ----------
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

# --------------------------------------------------------------------------
# ShardingSphere 分库分表配置 (如需使用)
# --------------------------------------------------------------------------
spring:
  shardingsphere:
    datasource:
      names: ds0,ds1
      ds0:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        jdbc-url: jdbc:mysql://localhost:3306/hkt_iot_device_0?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: root123456
      ds1:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        jdbc-url: jdbc:mysql://localhost:3306/hkt_iot_device_1?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: root123456
    rules:
      sharding:
        tables:
          device:
            actual-data-nodes: ds$->{0..1}.device
            table-strategy:
              standard:
                sharding-column: id
                sharding-algorithm-name: device-mod
        sharding-algorithms:
          device-mod:
            type: MOD
            props:
              sharding-count: 2

# --------------------------------------------------------------------------
# Flyway 数据库迁移配置 (可选)
# --------------------------------------------------------------------------
spring:
  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
    encoding: UTF-8
