# ============================================================================
# 华宽通智能体系统 - Redis 7.2 配置
# 版本: V1.0
# 说明: 针对IoT平台优化的Redis配置
# ============================================================================

# ---------------------------------------------------------------------------
# 网络配置
# ---------------------------------------------------------------------------
# 监听地址（允许外部访问）
bind 0.0.0.0
# 监听端口
port 6379
# TCP监听队列长度
tcp-backlog 511
# 超时时间（秒），0表示永不超时
timeout 0
# TCP keepalive
tcp-keepalive 300

# ---------------------------------------------------------------------------
# 通用配置
# ---------------------------------------------------------------------------
# 以守护进程运行（Docker环境设为no）
daemonize no
# PID文件
pidfile /var/run/redis_6379.pid
# 日志级别：debug/verbose/notice/warning
loglevel notice
# 日志文件（Docker环境输出到stdout）
logfile ""
# 数据库数量
databases 16

# ---------------------------------------------------------------------------
# 快照配置（RDB持久化）
# ---------------------------------------------------------------------------
# 900秒内至少有1个key变化则保存
save 900 1
# 300秒内至少有10个key变化则保存
save 300 10
# 60秒内至少有10000个key变化则保存
save 60 10000
# RDB文件名
dbfilename dump.rdb
# RDB文件目录
dir /data
# 如果保存失败是否停止写入
stop-writes-on-bgsave-error yes
# 是否压缩RDB文件
rdbcompression yes
# RDB校验和
rdbchecksum yes

# ---------------------------------------------------------------------------
# AOF配置（AOF持久化）
# ---------------------------------------------------------------------------
# 是否启用AOF
appendonly yes
# AOF文件名
appendfilename "appendonly.aof"
# AOF同步策略：always/everysec/no
appendfsync everysec
# AOF重写时是否同步
no-appendfsync-on-rewrite no
# AOF重写触发条件
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb
# AOF加载时是否忽略最后一条可能损坏的命令
aof-load-truncated yes
# AOF-RDB混合持久化
aof-use-rdb-preamble yes

# ---------------------------------------------------------------------------
# 内存管理
# ---------------------------------------------------------------------------
# 最大内存限制（根据实际情况调整，这里设置为2GB）
maxmemory 2gb
# 内存淘汰策略：allkeys-lru（删除最少使用的key）
maxmemory-policy allkeys-lru
# 内存淘汰算法采样数量
maxmemory-samples 5

# ---------------------------------------------------------------------------
# 慢查询日志
# ---------------------------------------------------------------------------
# 慢查询阈值（微秒）
slowlog-log-slower-than 10000
# 慢查询日志最大长度
slowlog-max-len 128

# ---------------------------------------------------------------------------
# 延迟监控
# ---------------------------------------------------------------------------
# 延迟监控阈值（毫秒）
latency-monitor-threshold 100

# ---------------------------------------------------------------------------
# 高级配置
# ---------------------------------------------------------------------------
# Hash数据类型优化
hash-max-ziplist-entries 512
hash-max-ziplist-value 64
# List数据类型优化
list-max-ziplist-size -2
list-compress-depth 0
# Set数据类型优化
set-max-intset-entries 512
# Sorted Set数据类型优化
zset-max-ziplist-entries 128
zset-max-ziplist-value 64
# HyperLogLog优化
hll-sparse-max-bytes 3000
# Stream数据类型优化
stream-node-max-bytes 4096
stream-node-max-entries 100

# ---------------------------------------------------------------------------
# 安全配置
# ---------------------------------------------------------------------------
# 访问密码（生产环境必须设置）
# requirepass your_strong_password_here
# 重命名危险命令（安全加固）
# rename-command CONFIG ""
# rename-command FLUSHDB ""
# rename-command FLUSHALL ""

# ---------------------------------------------------------------------------
# 主从复制（如需配置主从可取消注释）
# ---------------------------------------------------------------------------
# replica-serve-stale-data yes
# replica-read-only yes
# repl-diskless-sync no
# repl-diskless-sync-delay 5
# repl-disable-tcp-nodelay no
# replica-priority 100

# ---------------------------------------------------------------------------
# 集群配置（如需配置集群可取消注释）
# ---------------------------------------------------------------------------
# cluster-enabled yes
# cluster-config-file nodes-6379.conf
# cluster-node-timeout 15000
# cluster-replica-validity-factor 10
# cluster-migration-barrier 1
# cluster-require-full-coverage yes

# ---------------------------------------------------------------------------
# 其他配置
# ---------------------------------------------------------------------------
# 是否启用Lazy Free
lazyfree-lazy-eviction no
lazyfree-lazy-expire no
lazyfree-lazy-server-del no
replica-lazy-flush no
# 是否启用动态Hertz
dynamic-hz yes
# 是否启用活跃防御
active-defrag-cycle-min 1
active-defrag-cycle-max 25
active-defrag-threshold-lower 10
active-defrag-threshold-upper 100
