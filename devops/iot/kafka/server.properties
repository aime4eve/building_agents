# =============================================================================
# Kafka Broker 配置文件 - 设备接入层
# =============================================================================
# 版本: 3.6.0
# Broker ID
broker.id=1

# ---------------------------------------------------------------------------
# 网络配置
# ---------------------------------------------------------------------------
# 监听器配置
listeners=PLAINTEXT://:9092
advertised.listeners=PLAINTEXT://kafka1:9092
listener.security.protocol.map=PLAINTEXT:PLAINTEXT,SSL:SSL,SASL_PLAINTEXT:SASL_PLAINTEXT,SASL_SSL:SASL_SSL

# 线程配置
num.network.threads=8
num.io.threads=16

# 发送缓冲区
socket.send.buffer.bytes=102400
socket.receive.buffer.bytes=102400
socket.request.max.bytes=104857600

# ---------------------------------------------------------------------------
# 日志配置
# ---------------------------------------------------------------------------
# 日志目录
log.dirs=/kafka/logs

# 日志段配置
log.segment.bytes=1073741824
log.retention.hours=168
log.retention.check.interval.ms=300000
log.retention.bytes=-1

# 日志清理策略
log.cleanup.policy=delete

# 日志索引配置
log.index.size.max.bytes=10485760
log.index.interval.bytes=4096

# ---------------------------------------------------------------------------
# 副本配置
# ---------------------------------------------------------------------------
# 默认副本因子
default.replication.factor=3

# 最小同步副本数
min.insync.replicas=2

# 副本高水位
replica.high.watermark.checkpoint.interval.ms=5000

# 副本套接字超时
replica.socket.timeout.ms=30000
replica.socket.receive.buffer.bytes=65536
replica.lag.time.max.ms=30000

# ---------------------------------------------------------------------------
# ZooKeeper配置
# ---------------------------------------------------------------------------
zookeeper.connect=zookeeper1:2181,zookeeper2:2181,zookeeper3:2181
zookeeper.connection.timeout.ms=18000
zookeeper.session.timeout.ms=18000

# ---------------------------------------------------------------------------
# 事务配置
# ---------------------------------------------------------------------------
transaction.state.log.replication.factor=3
transaction.state.log.min.isr=2
transaction.state.log.num.partitions=50

# ---------------------------------------------------------------------------
# 组协调器配置
# ---------------------------------------------------------------------------
group.initial.rebalance.delay.ms=3000
group.min.session.timeout.ms=6000
group.max.session.timeout.ms=300000

# ---------------------------------------------------------------------------
# 自动创建Topic配置
# ---------------------------------------------------------------------------
auto.create.topics.enable=true
num.partitions=50
default.replication.factor=3

# ---------------------------------------------------------------------------
# 压缩配置
# ---------------------------------------------------------------------------
compression.type=snappy

# ---------------------------------------------------------------------------
# 日志刷新配置
# ---------------------------------------------------------------------------
log.flush.interval.messages=10000
log.flush.interval.ms=1000

# ---------------------------------------------------------------------------
# 监控配置
# ---------------------------------------------------------------------------
# JMX端口
# 启用JMX监控，便于Prometheus采集指标
kafka.metrics.reporters=io.strimzi.kafka.metrics.YammerMetricsReporter
kafka.metrics.polling.interval.secs=10
kafka.metrics.reporters.include=kafka.consumer:kafka.producer:kafka.server

# ---------------------------------------------------------------------------
# 安全配置（开发环境）
# ---------------------------------------------------------------------------
# 禁用安全（开发环境）
authorizer.class.name=kafka.security.authorizer.AclAuthorizer
allow.everyone.if.no.acl.found=true

# ---------------------------------------------------------------------------
# 性能优化配置
# ---------------------------------------------------------------------------
# 批处理配置
batch.size=16384
linger.ms=10

# 缓冲区配置
buffer.memory=67108864

# 最大请求大小
max.request.size=10485760

# ---------------------------------------------------------------------------
# 设备数据相关配置
# ---------------------------------------------------------------------------
# 增加Topic分区数（可根据设备数量调整）
num.partitions=50

# 减少fetch等待时间（低延迟场景）
fetch.max.wait.ms=100

# 增加副本最大延迟
replica.fetch.max.bytes=1048576
replica.fetch.wait.max.ms=500

# ---------------------------------------------------------------------------
# QUOTA配置（流量配额）
# ---------------------------------------------------------------------------
# 动态配额配置
quota.producer.default=10485760
quota.consumer.default=10485760
quota.window.size.seconds=1

# ---------------------------------------------------------------------------
# 连接配置
# ---------------------------------------------------------------------------
max.connections=100000
max.connections.per.ip=10000
max.connections.per.ip.overrides=
