# =============================================================================
# EMQX 配置文件 - 设备接入层
# =============================================================================
# 版本: 5.5.0
# 说明: 根据设备接入层架构设计说明书配置
# =============================================================================

# ---------------------------------------------------------------------------
# 节点配置
# ---------------------------------------------------------------------------
node {
  name = "emqx@127.0.0.1"
  cookie = "emqxsecretcookie2024"
  data_dir = "/opt/emqx/data"
  log_dir = "/opt/emqx/log"
}

# ---------------------------------------------------------------------------
# 集群配置
# ---------------------------------------------------------------------------
cluster {
  name = emqxcl
  discovery_strategy = manual
  # 手动集群模式，生产环境可配置为 etcd 或 kubernetes
  # Etcd配置示例:
  # discovery_strategy = etcd
  # etcd {
  #   server = "http://etcd1:2379,http://etcd2:2379,http://etcd3:2379"
  #   prefix = "emqxcl"
  #   node_ttl = 1m
  # }
}

# ---------------------------------------------------------------------------
# 监听器配置
# ---------------------------------------------------------------------------
listeners {
  # MQTT/TCP 监听器 (1883)
  tcp.default {
    bind = "0.0.0.0:1883"
    acceptors = 64
    max_connections = 1024000
    max_conn_rate = 1000
    access_rules = [
      "allow all"
    ]
  }

  # MQTT/SSL 监听器 (8883) - 生产环境推荐使用
  ssl.external {
    bind = "0.0.0.0:8883"
    acceptors = 32
    max_connections = 512000
    max_conn_rate = 500
    # TLS配置
    ssl_options {
      keyfile = "/etc/certs/key.pem"
      certfile = "/etc/certs/cert.pem"
      cacertfile = "/etc/certs/cacert.pem"
      verify = verify_peer
      fail_if_no_peer_cert = true
    }
    access_rules = [
      "allow all"
    ]
  }

  # MQTT/WebSocket 监听器 (8083)
  ws.default {
    bind = "0.0.0.0:8083"
    acceptors = 16
    max_connections = 102400
    max_frame_size = 64KB
    mqtt_path = "/mqtt"
    access_rules = [
      "allow all"
    ]
  }

  # MQTT/WebSocket/SSL 监听器 (8084)
  wss.external {
    bind = "0.0.0.0:8084"
    acceptors = 16
    max_connections = 51200
    max_frame_size = 64KB
    mqtt_path = "/mqtt"
    # TLS配置
    ssl_options {
      keyfile = "/etc/certs/key.pem"
      certfile = "/etc/certs/cert.pem"
      cacertfile = "/etc/certs/cacert.pem"
    }
    access_rules = [
      "allow all"
    ]
  }

  # QUIC监听器 (可选，用于未来扩展)
  quic.default {
    bind = "0.0.0.0:14567"
    acceptors = 8
    max_connections = 10240
    ssl_options {
      keyfile = "/etc/certs/key.pem"
      certfile = "/etc/certs/cert.pem"
    }
  }
}

# ---------------------------------------------------------------------------
# Zone 配置
# ---------------------------------------------------------------------------
zones {
  # 默认Zone配置
  default {
    # 会话配置
    session {
      max_subscriptions = 1024
      upgrade_qos = false
      max_awaiting_rel = 100
      await_rel_timeout = 300s
    }

    # 队列配置
    queue {
      type = simple
      max_length = 10000
      deliver_mode = real
      priorities = none
    }

    # 流控配置
    mqtt {
      max_packet_size = 1MB
      max_clientid_len = 256
      max_topic_levels = 128
      max_topic_alias = 65535
      retain_available = true
      wildcard_subscription = true
      shared_subscription = true
      exclusive_subscribe = false
      keepalive_multiplier = 1.5
    }
  }

  # 外部Zone配置（设备连接）
  external {
    # 访问控制规则
    acl_nomatch = deny
    acl_deny_action = disconnect

    # 强制会话抢占
    force_gc_policy = true

    # 并发限制
    max_connections = 1024000
    max_subscriptions = 200

    # 消息限流
    publish_limit = {
      max_connections = 1024000
      route_blocklist = "$SYS/#"
    }
  }
}

# ---------------------------------------------------------------------------
# 认证配置
# ---------------------------------------------------------------------------
authentication {
  # JWT认证（生产环境）
  # 1. 将JWT token作为username字段发送
  # 2. password字段可以是任意值
  {
    backend = jwt
    algorithm = rs256
    secret = "${JWT_SECRET}"
    # 从username字段获取JWT
    from = username
    # 验证payload中的字段
    verify_claims = {
      "device_id" = {optional = false}
      "tenant_id" = {optional = false}
      "device_type" = {optional = false}
    }
    # Token过期时间检查
    check_expiration = true
  }

  # 备用认证方式：HTTP认证（开发环境）
  # {
  #   backend = http
  #   method = post
  #   url = "http://device-service:8080/api/v1/auth/device/authenticate"
  #   headers {
  #     "Content-Type" = "application/json"
  #     "X-Auth-Type" = "MQTT"
  #   }
  #   body {
  #     with_clientid = true
  #     with_username = true
  #     with_password = true
  #   }
  # }
}

# ---------------------------------------------------------------------------
# ACL配置（访问控制列表）
# ---------------------------------------------------------------------------
acl {
  # 默认行为：拒绝
  nomatch = deny

  # ACL缓存
  cache {
    enable = true
    max_size = 32000
    ttl = 1m
  }

  # 内置ACL规则
  # 设备只能发布和订阅自己相关的Topic
  # Topic格式: device/{tenantId}/{deviceType}/{deviceId}/{messageType}
  # 设备只能访问: device/{tenantId}/*/{deviceId}/*
  #
  # 注意：生产环境应通过HTTP ACL动态加载规则
}

# ---------------------------------------------------------------------------
# 规则引擎配置
# ---------------------------------------------------------------------------
rules {
  # 规则引擎开关
  sys_topics = {
    enable = true
  }

  # 事件发布配置
  events {
    client_connected {
      action {
        topic = "$events/client_connected"
        payload = "${payload}"
        qos = 1
        retain = false
      }
    }

    client_disconnected {
      action {
        topic = "$events/client_disconnected"
        payload = "${payload}"
        qos = 1
        retain = false
      }
    }
  }
}

# ---------------------------------------------------------------------------
# Kafka桥接配置
# ---------------------------------------------------------------------------
bridges.kafka {
  # Kafka Producer配置 - 将设备数据转发到Kafka
  device_telemetry_producer {
    enable = true
    bootstrap_servers = "kafka1:9092,kafka2:9093,kafka3:9094"
    # Kafka Socket设置
    socket {
      ssl_options {
        enable = false
      }
    }
    # Kafka连接配置
    connect_timeout = 5s
    # 生产者配置
    provisioning {
      max_batch_bytes = 896KB
      compression = snappy
      min_buffer_delay = 0ms
    }

    # Kafka主题映射
    # 将MQTT消息转发到Kafka Topic
    routes = [
      {
        # 遥测数据Topic
        topic = "device-telemetry"
        # MQTT Topic过滤器
        from = "device/+/+/+/telemetry"
        # 消息模板
        payload = "${payload}"
        # QoS映射
        qos = 1
        # Kafka分区策略：按deviceId哈希
        key = "${clientid}"
      }
      ,
      {
        # 设备事件Topic
        topic = "device-event"
        from = "device/+/+/+/event"
        payload = "${payload}"
        qos = 1
        key = "${clientid}"
      }
      ,
      {
        # 设备状态Topic
        topic = "device-status"
        from = "device/+/+/+/status"
        payload = "${payload}"
        qos = 1
        key = "${clientid}"
      }
      ,
      {
        # 设备心跳Topic
        topic = "device-heartbeat"
        from = "device/+/+/+/heartbeat"
        payload = "${payload}"
        qos = 0
        key = "${clientid}"
      }
    ]

    # Kafka消息配置
    kafka {
      # Kafka消息头
      headers {
        # 添加设备ID
        device_id = "${clientid}"
        # 添加租户ID（从Topic提取）
        tenant_id = "${topic.split('/')[1]}"
        # 添加消息类型
        message_type = "${topic.split('/')[4]}"
      }
    }
  }

  # Kafka Consumer配置 - 从Kafka接收平台下发的命令
  device_command_consumer {
    enable = true
    bootstrap_servers = "kafka1:9092,kafka2:9093,kafka3:9094"
    connect_timeout = 5s

    # 消费者配置
    provisioning {
      auto_offset_reset = latest
      max_batch_bytes = 896KB
      min_buffer_delay = 0ms
    }

    # 订阅Kafka Topic并转发到MQTT
    routes = [
      {
        # 订阅命令Topic
        topic = "device-command"
        # MQTT目标Topic
        forward_to = "device/${headers.tenant_id}/${headers.device_type}/${headers.device_id}/command"
        # 消息处理模板
        payload = "${payload}"
        # QoS
        qos = 1
      }
    ]
  }
}

# ---------------------------------------------------------------------------
# Dashboard配置
# ---------------------------------------------------------------------------
dashboard {
  # 监听器配置
  listeners.http {
    bind = "18083"
    num_acceptors = 4
    max_connections = 512
  }

  # 默认用户名密码
  default_username = "admin"
  default_password = "admin123456"

  # 示例间隔（秒）
  sample_interval = 10s

  # UUID（用于集群部署）
  # UUID在首次启动时自动生成
}

# ---------------------------------------------------------------------------
# Prometheus监控配置
# ---------------------------------------------------------------------------
prometheus {
  # 推送网关配置（可选）
  push_gateway_server = "http://prometheus:9091"

  # 拉取间隔（毫秒）
  interval = 15000

  # Prometheus Headers（可选）
  headers = {}

  # VM数据分布
  vm_dist = "auto"
}

# ---------------------------------------------------------------------------
# 日志配置
# ---------------------------------------------------------------------------
log {
  # 控制台日志
  console_handler {
    enable = true
    level = info
    formatter = text
    # 日志格式
    single_line = true
  }

  # 文件日志
  file_handler {
    enable = true
    level = warning
    # 日志文件路径
    dir = "/opt/emqx/log"
    # 日志轮转大小
    rotation_size = 50MB
    # 日志轮转数量
    rotation_count = 10
    # 日志格式
    formatter = json
  }

  # 日志级别调整
  # 可以针对特定模块设置不同的日志级别
  # level {
  #   emqx_channels = debug
  #   emqx_connection = debug
  # }
}

# ---------------------------------------------------------------------------
# 消息持久化配置（可选）
# ---------------------------------------------------------------------------
# message_storage {
#   # 启用消息持久化
#   enable = true
#   # 存储类型
#   type = "redis"
#   # Redis配置
#   redis {
#     # Redis服务器
#     server = "redis:6379"
#     # Redis数据库
#     database = 0
#     # Redis Key前缀
#     key_prefix = "emqx:message:"
#     # 消息过期时间（秒）
#     expire_time = 3600
#   }
# }

# ---------------------------------------------------------------------------
# 延迟发布配置
# ---------------------------------------------------------------------------
delayed {
  # 启用延迟发布
  enable = true
  # 最大延迟时间（秒）
  max_delayed_messages = 10000
}

# ---------------------------------------------------------------------------
# 其他配置
# ---------------------------------------------------------------------------
# 系统监控
system_monitor {
  # 启用系统监控
  enable = true

  # 长时间GC告警阈值（毫秒）
  long_gc = 1000

  # 长调度告警阈值（毫秒）
  long_schedule = 500

  # 大堆分配告警阈值（字节）
  large_heap_alloc = 32MB

  # 事件队列长度告警阈值
  event_queue_length = 1000

  # 消息队列长度告警阈值
  message_queue_length = 1000
}
