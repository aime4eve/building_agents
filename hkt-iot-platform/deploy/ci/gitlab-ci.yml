# GitLab CI/CD Pipeline for HKT IoT Platform

stages:
  - build
  - test
  - quality
  - package
  - deploy

variables:
  DOCKER_REGISTRY: harbor.hkt-iot.com
  DOCKER_IMAGE_PREFIX: hkt-iot
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository"
  KUBECONFIG: /tmp/kubeconfig

# Maven缓存
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - .m2/repository/

# 构建阶段
build:
  stage: build
  image: maven:3.9-eclipse-temurin-17
  script:
    - echo "Building project..."
    - mvn clean compile -DskipTests
  artifacts:
    paths:
      - target/
    expire_in: 1 hour
  only:
    - merge_requests
    - main
    - develop

# 单元测试
test:
  stage: test
  image: maven:3.9-eclipse-temurin-17
  services:
    - mysql:8.0
    - redis:7-alpine
  variables:
    MYSQL_ROOT_PASSWORD: root
    SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/test_db
    SPRING_DATA_REDIS_HOST: redis
  script:
    - echo "Running tests..."
    - mvn test
  coverage: '/Total.*?([0-9]{1,3})%/'
  artifacts:
    reports:
      junit: '**/target/surefire-reports/TEST-*.xml'
    paths:
      - target/site/jacoco/
    expire_in: 1 week
  only:
    - merge_requests
    - main
    - develop

# 代码质量检查
sonarqube:
  stage: quality
  image: maven:3.9-eclipse-temurin-17
  script:
    - mvn sonar:sonar
      -Dsonar.host.url=${SONAR_HOST}
      -Dsonar.login=${SONAR_TOKEN}
      -Dsonar.gitlab.project_id=${CI_PROJECT_ID}
      -Dsonar.gitlab.commit_sha=${CI_COMMIT_SHA}
      -Dsonar.gitlab.ref_name=${CI_COMMIT_REF_NAME}
  allow_failure: true
  only:
    - merge_requests
    - main
    - develop

# 构建Docker镜像
docker-build:
  stage: package
  image: docker:24-dind
  services:
    - docker:24-dind
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin $DOCKER_REGISTRY
  script:
    - |
      for service in user-service device-service notification-service rule-service scene-service smart-apps-service; do
        if [ -d "$service" ]; then
          echo "Building Docker image for $service..."
          docker build -t $DOCKER_REGISTRY/$DOCKER_IMAGE_PREFIX/$service:$CI_COMMIT_TAG $service/
          docker push $DOCKER_REGISTRY/$DOCKER_IMAGE_PREFIX/$service:$CI_COMMIT_TAG
        fi
      done
  only:
    - tags

# 部署到开发环境
deploy:dev:
  stage: deploy
  image: bitnami/kubectl:latest
  environment:
    name: development
    url: https://dev.hkt-iot.com
  script:
    - kubectl set image deployment/user-service user-service=$DOCKER_REGISTRY/$DOCKER_IMAGE_PREFIX/user-service:$CI_COMMIT_TAG -n hkt-iot-dev
    - kubectl rollout status deployment/user-service -n hkt-iot-dev
  only:
    - develop
  when: manual

# 部署到生产环境
deploy:prod:
  stage: deploy
  image: bitnami/kubectl:latest
  environment:
    name: production
    url: https://hkt-iot.com
  script:
    - kubectl set image deployment/user-service user-service=$DOCKER_REGISTRY/$DOCKER_IMAGE_PREFIX/user-service:$CI_COMMIT_TAG -n hkt-iot
    - kubectl rollout status deployment/user-service -n hkt-iot
  only:
    - /^v\d+\.\d+\.\d+$/
  when: manual
