## 设备接入服务配置
server:
  port: 8084
  servlet:
    context-path: /api/v1/device-ingestion

spring:
  application:
    name: hkt-iot-device-ingestion-service

  ## Kafka配置
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: device-ingestion-service
      auto-offset-reset: latest
      enable-auto-commit: false
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      max-poll-records: 100
      properties:
        session.timeout.ms: 30000
        heartbeat.interval.ms: 10000
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3
      batch-size: 16384
      linger-ms: 10

  ## Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
        timeout: 3000ms

  ## 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hkt_iot?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000

## InfluxDB配置
influx:
  url: http://localhost:8086
  token: hkt-iot-token-2024
  org: hkt_iot
  bucket:
    raw: hkt_iot_raw
    event: hkt_iot_event
    status: hkt_iot_status
  write:
    batch-size: 1000
    flush-interval: 1000
    buffer-limit: 10000

## MQTT客户端配置
mqtt:
  broker:
    url: tcp://localhost:1883
    client-id: hkt-ingestion-${random.value}
    username: hkt_ingestion
    password: hkt_ingestion_password
    connection-timeout: 10
    keep-alive-interval: 60
    automatic-reconnect: true
  topics:
    telemetry: device/+/+/+/telemetry
    event: device/+/+/+/event
    status: device/+/+/+/status
    heartbeat: device/+/+/+/heartbeat
  qos: 1
  completion-timeout: 30000

## 设备处理配置
device:
  heartbeat:
    interval: 60
    timeout: 180
  cache:
    ttl: 300
  idempotency:
    telemetry-ttl: 3600
    event-ttl: 604800

## Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}

## 日志配置
logging:
  level:
    root: INFO
    com.hkt.iot.ingestion: DEBUG
    org.springframework.kafka: INFO
    org.apache.kafka: WARN
    org.eclipse.paho: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
