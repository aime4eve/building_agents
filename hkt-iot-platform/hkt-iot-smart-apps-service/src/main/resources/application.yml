server:
  port: 8085

spring:
  application:
    name: hkt-iot-smart-apps-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: ${NACOS_GROUP:HKT_IOT}
      config:
        server-addr: ${NACOS_ADDR:localhost:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: ${NACOS_GROUP:HKT_IOT}
        file-extension: yml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_ADDR:localhost:3306}/hkt_iot_smart_apps?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
  rabbitmq:
    host: ${RABBITMQ_ADDR:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    virtual-host: ${RABBITMQ_VHOST:/}
    listener:
      simple:
        acknowledge-mode: manual
        prefetch: 10
        retry:
          enabled: true
          max-attempts: 3
          initial-interval: 1000

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.hkt.iot.smartapps.infrastructure.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 日志配置
logging:
  level:
    root: INFO
    com.hkt.iot.smartapps: DEBUG
    com.hkt.iot.smartapps.domain: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n'

# 管理端点配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true

# Swagger/Knife4j配置
knife4j:
  enable: true
  setting:
    language: zh_cn
  production: false

# 智能应用配置
smart-apps:
  mold-prevention:
    # 防霉管控配置
    default-evaluation-interval: 300000  # 默认评估间隔（毫秒）
    report-generation-schedule: "0 0 6 * * ?"  # 每天早上6点生成报告
    risk-alert-threshold: HIGH  # 告警阈值
  smart-livestock:
    # 智慧畜牧配置
    geofence-check-interval: 60000  # 电子围栏检测间隔（毫秒）
    health-check-interval: 3600000  # 健康检查间隔（毫秒）
    location-tracking-interval: 300000  # 位置追踪间隔（毫秒）
    violation-alert-enabled: true  # 越界告警开关
